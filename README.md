# ⚙️ Booking API (Server Side)

Це серверна частина додатку для бронювання, побудована на Node.js та Express. Сервер відповідає за логіку роботи з користувачами, збереження зображень та управління базою записів.

---

## 🚀 Технологічний стек
- **Node.js** — середовище виконання JavaScript.
- **Express** — веб-фреймворк для створення API.
- **MongoDB** — NoSQL база даних для зберігання інформації.
- **Mongoose** — ORM для моделювання даних.
- **Cloudinary** — хмарний сервіс для зберігання та оптимізації фото профілів.
- **CORS** — налаштування доступу для взаємодії з фронтендом.

---

## 🛠 Налаштування та запуск

### 1. Встановлення залежностей:
```bash
npm install

2. Конфігурація середовища:
Створіть файл .env у корені папки backend та додайте ваші ключі:
PORT=5000
MONGO_URI=ваш_рядок_підключення_mongodb
CLOUDINARY_CLOUD_NAME=ваш_cloud_name
CLOUDINARY_API_KEY=ваш_api_key
CLOUDINARY_API_SECRET=ваш_api_secret

3. Запуск сервера:
Bash
# Режим розробки (з nodemon)
npm run dev
# Стандартний запуск
npm start

📡 API Ендпоінти
Користувачі (/users)
Метод,Ендпоінт,Опис
GET,/users,Отримати всіх користувачів
GET,/users/business,Отримати список тільки бізнес-акаунтів
POST,/users,Створити нового користувача (Client/Biz)
PUT,/users/:id,Оновити дані існуючого користувача
DELETE,/users/:id,Видалити користувача з бази

Бронювання (/bookings)
Метод,Ендпоінт,Опис
POST,/bookings,Створити нове бронювання
GET,/bookings,Отримати список усіх записів
GET,/bookings/user/:id,Отримати запис конкретного користувача

📂 Структура папок
models/ — схеми даних Mongoose (User, Booking).
routes/ — описи маршрутів API.
controllers/ — логіка обробки запитів (опціонально).
config/ — налаштування підключення до БД та Cloudinary.

🛡 Безпека та CORS
Сервер налаштований на роботу з певними доменами. При деплої на Render/Railway/Heroku не забудьте оновити білий список CORS, щоб ваш фронтенд на Vercel мав доступ до даних.
